<!DOCTYPE html>
<html lang="en">
<head>
  <!-- Add your head content here -->
</head>
<body>
  <%- include('header.ejs') %>

  <div class="Main">
    <h2>Modify Post</h2>
    <form method="POST" action="/update/<%= selectedPost.id %>?_method=PATCH">
      <label for="title">Title:</label>
      <input type="text" id="title" name="title" value="<%= selectedPost.title %>" required>

      <label for="description">Description:</label>
      <textarea id="description" name="description" required><%= selectedPost.description %></textarea>

      <label for="blog-content">Content:</label>
      <textarea id="blog-content" name="blog-content" required><%= selectedPost.content %></textarea>

      <button type="submit" class="btn btn-primary">Update Post</button>
    </form>
  </div>

  <!-- Add your script tags and other content here -->
</body>
</html>
